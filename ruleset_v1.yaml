custom:
  overwrite_original_rules: false
  enable_rule_generator: false
  
  proxy_groups: 
    - 自动选择`url-test`.*`http://www.gstatic.com/generate_204`300
    - 手动选择`select`.*
    - 自动屏蔽🐶网站`select`[]REJECT`[]DIRECT

